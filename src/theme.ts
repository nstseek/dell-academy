import { createTheme } from '@mui/material/styles';

export const dellTheme = createTheme({
    palette: {
      primary: {
        main: '#007db8',
      },
      secondary: {
        main: '#66c2ff',
      },
    },
  });